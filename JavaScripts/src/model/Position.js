export default class Position {
    constructor(top = 0, left = 0) {
        /**
         * @type {number}
         */
        this.top = top;

        /**
         * @type {number}
         */
        this.left = left;
    }
}
